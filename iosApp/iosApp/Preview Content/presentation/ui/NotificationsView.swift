import SwiftUI

struct NotificationsView: View {
    var body: some View {
        NavigationStack {
            Text("Notifications")
                .navigationTitle("Notifications")
        }
    }
}
